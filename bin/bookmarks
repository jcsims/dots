#!/usr/bin/env bash

# Using bash instead of fish here, because Hammerspoon launches this with a very
# limited shell (meaning e.g. Homebrew-installed software is not available on
# the PATH, like fish).

client=emacsclient

if [[ $(uname) = "Darwin" ]]; then
    client=/opt/homebrew/bin/emacsclient
fi

$client -ne "(present-open-bookmark-frame)"
